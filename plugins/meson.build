subdir('gedit-plugin')

install_data(
  'devhelp.el',
  install_dir: get_option('datadir') / 'emacs/site-lisp'
)

install_data(
  'devhelp.vim',
  install_dir: get_option('datadir') / 'vim/vimfiles/plugin'
)
